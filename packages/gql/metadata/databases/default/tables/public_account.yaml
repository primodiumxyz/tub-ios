table:
  name: account
  schema: public
array_relationships:
  - name: account_transactions
    using:
      foreign_key_constraint_on:
        column: account
        table:
          name: account_transaction
          schema: public
select_permissions:
  - role: public
    permission:
      columns:
        - id
        - username
        - created_at
      filter: {}
      allow_aggregations: true
    comment: ""
