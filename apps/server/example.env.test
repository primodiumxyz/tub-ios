# rename this file to .env.test for testing purposes.
# cd apps/server && pnpm test

NODE_ENV=local
GEYSER_URL=dummy-geyser-url # mock for testing

JUPITER_URL= # requires a valid Jupiter API key
PRIVY_APP_ID=dummy-privy-app-id # mock for testing
PRIVY_APP_SECRET=dummy-privy-secret # mock for testing

# fee payer
FEE_PAYER_PRIVATE_KEY="Your fee payer private key here"
TEST_USER_PRIVATE_KEY="Your test user private key here"

SERVER_HOST=0.0.0.0 # default
SERVER_PORT=8888 # default
HASURA_ADMIN_SECRET=password # mock for testing
GRAPHQL_URL="http://localhost:8090/v1/graphql" # default
JWT_SECRET=secret # mock for testing

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD= # optional

QUICKNODE_ENDPOINT=https://your-rpc-endpoint.com # RPC endpoint, no slash at the end
QUICKNODE_TOKEN=your-rpc-token # RPC token

# Coinbase CDP API, replace with a stringified JSON file with name and privateKey fields
# Both strings are escaped by the CDP JSON generated by coinbase. Parsing is needed in code before using.
COINBASE_CDP_API_KEY_NAME='' # not necessary for testing
COINBASE_CDP_API_KEY_PRIVATE_KEY='' # not necessary for testing









